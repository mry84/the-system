# The System

Private house ledger for David Hunt’s movie night. Not a product. Friends only.

Create a night, read the archive, search, and see crusades, Golden Children, and automatic badges.

## Local

```bash
npm install
npx prisma db push
npm run db:seed
npm run dev
```

Open http://localhost:3000
